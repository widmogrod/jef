;(function() {
var reactive_value, reactive_observable, reactive;
reactive_value = function value(v) {
  return function (set) {
    if (arguments.length) {
      v = set;
      return this;
    }
    return v;
  };
};
reactive_observable = function observable(value, fn) {
  return function (set, old) {
    if (arguments.length) {
      old = value();
      value(set);
      old !== set && fn(set, old);
      return this;
    }
    return value();
  };
};
reactive = function (value, observable) {
  
  return {
    value: value,
    observable: observable
  };
}(reactive_value, reactive_observable);window.jef = window.jef || {}; window.jef.reactive = reactive; 
}());